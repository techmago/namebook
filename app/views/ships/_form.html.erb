<%= form_for(@ship) do |f| %>
  <% if @ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ship.errors.count, "error") %> prohibited this ship from being saved:</h2>

      <ul>
      <% @ship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :descr %><br />
    <%= f.text_area :descr %>
  </div>
  <div class="field">
    <%= f.label :base_type_id %><br />
    <%= f.number_field :base_type_id %>
  </div>
  <div class="field">
    <%= f.label :ship_size_id %><br />
    <%= f.number_field :ship_size_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
