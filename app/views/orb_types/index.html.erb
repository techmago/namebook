<h1>Listing orb_types</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orb_types.each do |orb_type| %>
  <tr>
    <td><%= orb_type.id %></td>
    <td><%= orb_type.nome %></td>
    <td><%= link_to 'Detalhes', orb_type %></td>
    <td><%= link_to 'Editar', edit_orb_type_path(orb_type) %></td>
    <td><%= link_to 'Remover', orb_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Orb type', new_orb_type_path %>
